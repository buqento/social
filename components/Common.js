const Button = ({ children }) => {
    return (
        <div className="border text-center text-white font-medium bg-blue-500 rounded">
            {children}
        </div>
    )
}

export default Button